;NBA Jam - Multiplayer Server Configuration
;
;This file configures available servers for REAL-TIME multiplayer games.
;(For LORB character persistence, see server.ini instead)
;
;You can define multiple servers here. Each [section] is a server.
;Players will be able to choose which server to connect to.
;
;Set default=true on a server to auto-select it.

[local]
name=Futureland.today
addr=localhost
port=10088
description=Games with players on this BBS only
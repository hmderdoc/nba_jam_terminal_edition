Configuration files for NBA JAM

Place mp_config.ini and rosters.ini here if you move them from the game root.

Paths in code expect: js.exec_dir + "lib/config/mp_config.ini" and
js.exec_dir + "lib/config/rosters.ini" after the code change.
